package cmir2469.backend.domain.validators;

import cmir2469.backend.domain.UserDetail;

public class UserDetailValidator implements Validator<UserDetail>  {
    @Override
    public void validate(UserDetail entity) throws ValidationException {
//        String message = "";
//        if(entity.getFirstName() == null || entity.getFirstName().equals(""))
//            message += "Invalid first name!\n";
//        if(entity.getLastName() == null || entity.getLastName().equals(""))
//            message += "Invalid last name!\n";
//        if(entity.getBirthday() == null)
//            message += "Invalid birthday!\n";
//        if(entity.getDescription() == null || entity.getDescription().equals(""))
//            message += "Invalid description!\n";
//        if (!message.equals("")) {
//            throw new Validator.ValidationException(message);
//        }
    }
}
